#include <stdio.h>

int main(void)
{
    int num;
    printf("enter number to check: ");
    scanf("%d", &num);

    for (int i = 0; i < num; i++)
    {
        if (num % i == 0)
        {
            
        }
    }
}